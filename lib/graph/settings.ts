/**
 * lib/graph/settings.ts — DEPRECATED / SAFE TO DELETE
 *
 * The old single-outlet settings module was replaced by the per-outlet
 * lib/graph/outlet-settings.ts under the v2 (multi-outlet) schema. Nothing
 * imports this file anymore. It's kept only as an inert placeholder so an
 * extract-over-existing-files update doesn't leave the old, now-broken code
 * behind. You can delete this file whenever convenient.
 */
export {};
